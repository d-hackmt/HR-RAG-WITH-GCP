"""HR Policy Assistant package.

Files are numbered in reading order — start at config.py (01) and follow
the numbers. Ingestion is 01–09, the query pipeline 10–17, then
observability / eval, then the entry-point scripts at the repo root.
"""
