# Secrets — live credentials, never commit
.env

# Python bytecode / caches
**/__pycache__/
**/*.pyc
**/*.pyo
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Virtualenvs
hrrenv/
hrenv/
genenv/
.venv/
venv/
.venv_eval/


# OS / editor cruft
.DS_Store
Thumbs.db

# Claude Code tooling — local only, not in the repo
.claude/

# Local, secret-bearing / machine-specific — never commit
.streamlit/secrets.toml
deploy.env.yaml
plan.csv
commands-basic-rag.md
commands-security.md
commands-deployment.md
commands/commands-basic-rag.md
commands/commands-security.md
commands/commands-deployment.md
